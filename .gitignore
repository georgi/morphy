node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
.turbo/
coverage/

# Stray TS build output that must never leak next to the @chess/shared source
# (the real build emits to packages/shared/dist/). Keep the contract single-source.
packages/shared/src/*.js
packages/shared/src/*.js.map
packages/shared/src/*.d.ts
